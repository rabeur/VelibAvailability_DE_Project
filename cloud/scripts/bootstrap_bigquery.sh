#!/usr/bin/env bash
# Create the initial BigQuery schemas (Silver tables) after terraform apply.
# Implemented in step 2 — keeps DDL idempotent so it can be re-run safely.
set -euo pipefail
echo "bootstrap_bigquery.sh: not implemented yet (step 2)"
exit 1
